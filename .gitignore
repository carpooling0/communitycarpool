# Environment files — never commit
.env
.env.*

# macOS
.DS_Store

# Scratch / local-only files
*.local
marketing/

# Supabase CLI local cache (project-ref, version stamps)
supabase/.temp/
**/supabase/.temp/
